# 需求

## 一句话

把参考插件 `baiyu858/PhyAgentOS-rekep-real-plugin` 的**仿真 ReKep 能力**，按 PhyAgentOS 最新的 Node/Forge 协议（见《参考手册》）重新组织成一个 **Skill**，接入 `PhyAgentOS-core`。本次**只做仿真，不做真机**。

## 背景

- 参考插件实现了完整的 ReKep（仿真 + 真机），但集成方式是**旧的 HAL 插件**（`hal_watchdog` + `ACTION.md` + driver），不符合当前 Forge/Skill 协议。
- `PhyAgentOS-core` 历史上也有过 ReKep 实现，随 Forge 架构替换被删除；参考插件是其外化维护版。
- 社区另有一个尝试 `rekep-paos`（算法硬编码、Node 形态过时），**仅作参考，不作依据**。

## 目标

- 产出一个可被 PAOS 安装/启动的 `manifest-v2` Skill：
  智能体读取 `SKILL.md` → 生成工具调用链 → `forge_gateway` 把调用路由到各 Node 执行。
- 仿真闭环：感知关键点 → VLM 生成约束（沙箱执行）→ 分层优化求解 → 执行 → 产出视频与结果。
- **模仿**参考实现的架构与算法，**不照搬**其 HAL 集成方式。

## 范围

- 只做仿真。底座最终采用 **MuJoCo + Dobot Nova2**（原计划优先 OmniGibson+Fetch，但 Isaac Sim pip 安装要求 glibc ≥ 2.35，本构建机 2.31 无法安装；经确认改用 MuJoCo，算法仍照搬参考实现）。
- 不做真机（参考插件的真机素材后续再议）。
- 不修改 `PhyAgentOS-core`、`rekep-paos` 仓库。

## 硬约束

- **不使用根盘 `/`、`/tmp`、`/root` 存储**（根盘仅剩约 4.7G）；所有状态、缓存、构建、下载一律放 `/data7`。
- 构建平台 = 本机 Ubuntu 20.04 / glibc 2.31（对目标 2.35 前向兼容）。
- 遵循 PhyAgentOS 最新协议：`manifest_version: 2`、单文件 `executable_tar_gz` Node、registry node lock（sha256）、Dora 0.4.1 / dora-message 0.7.0。
- VLM 生成的 Python 约束必须**沙箱执行**（AST 白名单 + 超时），保证安全与可复现。

## 交付物

- `需求.md`（本文件）
- 《开发手册》（P0）
- Node 源码、Skill bundle 源码、沙箱执行器、测试
- 《验收方案》与验收证据（含分阶段实验与总体试验）

## 里程碑

P0 手册 → P1 基线 → P2 内核回收 → P3 Forge 化 → P4 验收 → P5 打包发布。

- P0–P4 已完成并通过验收；真实 VLM 约束生成 + Agent/SKILL.md 端到端均已实拍 `succeeded`。
- P5（可移植单文件 node 与 Registry/TOS 发布）为可选，尚未进行。

## 验收要点

分阶段验收：平台与产物契约、算法 parity、Node/Tool 契约、沙箱与约束语义、SKILL/LLM 编排、端到端总体试验；每项均有可复现证据，并满足《参考手册》的闭环清单与异常场景。
