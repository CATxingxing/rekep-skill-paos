# URDF 资产

本目录归档 Dobot Nova2 6-DoF 的 URDF leaf、Xacro 和 mesh。可直接加载的静态文件为
`dobot_rviz/nova2_robot.urdf`，参数化来源为 `cra_description/nova2_robot.xacro`。
Robotiq 2F-85 描述位于独立的 `robotiq_2f_85_description/` leaf。

来源、许可证和 SHA256 记录在 `DOBOT_LICENSE` 及各上游目录中；校验入口为
`scripts/validate_assets.py`。
