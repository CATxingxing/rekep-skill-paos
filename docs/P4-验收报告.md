# P4 验收报告：需求覆盖与证据

日期：2026-09-21

## 结论

除**唯一一项需要你提供 API key 才能实拍**的「LLM 读 SKILL.md 生成调用链」外，需求已在本机端到端达成并通过验收；该项的代码路径已用 mock VLM 单测覆盖，只差联网实跑。

## 需求逐条对照

| 需求（`需求.md`） | 状态 | 证据 |
|---|---|---|
| 产出可被 PAOS 安装/启动的 manifest-v2 Skill | ✅ | `evidence/P4_run.log`：`State: running`；`evidence/P1/*` 契约验证 |
| LLM 读 SKILL.md → 生成工具调用链 → gateway 路由到 node | ⏳ 待 key | 运行时与 gateway 已真实验证；VLM 计划路径见 `tests/test_vlm_plan.py`（mock 通过）；`paos agent` 一条命令即可实拍 |
| 仿真闭环：感知→VLM约束(沙箱)→分层求解→执行→视频 | ✅ | `evidence/P4/probe.log` + `evidence/P4/rekep_mujoco.mp4`；`tests/test_ref_*`（算法对齐） |
| 模仿参考实现、不照搬；不改 PhyAgentOS-core/rekep-paos | ✅ | `reference/` submodule @ `c532a28` 原样引用；无对两仓库的修改 |
| 只做仿真 | ✅ | MuJoCo env；真机素材未接入 |
| 不用根盘 | ✅ | `scripts/env.sh` 强制 /data7；`find / -xdev -user ...` 无我们的近期写入 |
| glibc 2.31 构建机 / 2.35 目标兼容 | ✅ | `scripts/shim/`；本机 Ubuntu 20.04 |
| 单文件 `executable_tar_gz` node + sha256 lock | ✅ | `scripts/build_skill.sh`、`install_nodes_local.sh`；node lock 校验通过 |
| VLM 约束沙箱执行 | ✅ | `src/rekep_sim/sandbox.py`；`tests/test_sandbox.py`（11 负例） |
| 参考算法原样引用 | ✅ | `src/rekep_sim/refimpl.py`（本地 DINOv2 + 有界 kmeans 守卫，不改上游） |

## 验收实验（A–F）

- **A 平台/产物契约**：P1 smoke（package→install→forge-node install/verify→inspect）全过；P4 安装/启动成功。
- **B 参考对齐**：`pytest` 25 passed（R1 keypoint / R2 解析 / R3 solver / R5 utils）。
- **C Node/Tool 契约**：`/tools` 与三个 context ready；query/action/session 生命周期正常。
- **D 沙箱与约束语义**：合法加载、11 类非法程序拒绝。
- **E SKILL/LLM 编排**：运行时/网关真实调用链通过；LLM 驱动待 key（代码路径 mock 覆盖）。
- **F 端到端总体**：`succeeded`，落点误差 0.0353 m，产出 MP4；负例 `OBSERVATION_STALE / MOTION_NOT_ALLOWED / PLAN_TAMPERED / PLAN_NOT_FOUND` 全部 fail-closed。

## 复现

```bash
source scripts/env.sh
bash scripts/nsrun.sh bash scripts/acceptance_full.sh   # 输出 P4_ACCEPTANCE: PASS
```

## 待办（非阻塞）

1. **E 实拍**：提供 VLM key 后执行
   ```bash
   export REKEP_VLM_API_KEY=...   # 并 REKEP_VLM_BASE_URL / REKEP_VLM_MODEL
   # 将 profile 环境 REKEP_PLAN_SOURCE 改为 vlm，再 paos agent -m '...'
   ```
2. **P5（可选）**：当前 node 入口为宿主路径的 shell 包装（本地安装/启动可行）；生产分发需薄冻结/编译为可移植单文件，并按 Registry MR 流程登记（`skill.yaml` 已生成完整 lock）。

## 关键修复记录（本机）
共享 dora 端口冲突（私有 netns 隔离）；glibc<2.32 chmod shim；gateway `agent` 键被拒；forge_tool session 不可 cancellable；execute node 惰性初始化；IK 多 seed；assisted grasp；**kmeans 无界死循环→有界实现**（曾致 30 分钟挂起）。
