# P4 验收报告：需求覆盖与证据

日期：2026-09-21（更新：真实 VLM + Agent 端到端完成后）

## 结论

**需求已满足**：Skill 可被 PAOS 安装/启动，智能体读 `SKILL.md` 生成工具调用链，`forge_gateway` 路由到各 Node，完成「感知 → VLM 生成约束（AST 沙箱）→ 分层求解 → MuJoCo 执行 → 视频/结果」闭环，AgentTask 判定 `succeeded`。

## 需求逐条对照

| 需求（`需求.md`） | 状态 | 证据 |
|---|---|---|
| PAOS 安装/启动 `manifest-v2` Skill | ✅ | `paos skill start` → running；`/tools` + 三个 context ready（`evidence/P4_run.log`、`evidence/P5v/agent_shim6.log`） |
| 智能体读 `SKILL.md` → 工具链 → gateway 路由到 node | ✅ | `paos agent` 端到端：`task_c731062872774168: succeeded`（perception→plan→execute，`evidence/P5v/agent_shim6.log`） |
| 仿真闭环：感知→**VLM 生成约束**(沙箱)→分层求解→执行→视频 | ✅ | 真模型 `deepseek-v4-flash-vision-exp`：`exp_vlm3.log`（place_error 0.022m）+ Agent 运行（place_error 0.0353m，435 帧 MP4，`evidence/P5v/agent_result.mp4`） |
| 模仿参考实现、不照搬；不改 `PhyAgentOS-core`/`rekep-paos` | ✅ | `reference/` submodule @ `c532a28` 原样引用；两仓库 `git status` 干净 |
| 只做仿真 | ✅ | MuJoCo env；真机素材未接入 |
| 不用根盘 | ✅ | `scripts/env.sh` 强制 /data7；`find / -xdev -user ...` 无我们的近期写入 |
| 单文件 `executable_tar_gz` node + sha256 lock | ✅ | `scripts/build_skill.sh`、`install_nodes_local.sh`；lock + receipt 校验通过 |
| VLM 约束沙箱执行 | ✅ | `src/rekep_sim/sandbox.py`；`tests/test_sandbox.py`（11 负例） |
| 参考算法原样引用（不改上游） | ✅ | `src/rekep_sim/refimpl.py`（本地 DINOv2 注入 + 有界 kmeans / n_jobs=1 / 线程上限等运行时加固） |

## 验收实验（A–F）

- **A 平台/产物契约**：P1 smoke（package→install→forge-node install/verify→inspect）全过；P4 安装/启动成功。
- **B 参考对齐**：`pytest` **26 passed**（R1 keypoint / R2 解析 / R3 solver / R5 utils / 沙箱 / VLM 计划路径 mock）。
- **C Node/Tool 契约**：`/tools` 与三个 context ready；query/action/session 生命周期正常。
- **D 沙箱与约束语义**：合法加载、11 类非法程序拒绝。
- **E SKILL/LLM 编排**：`paos agent` 真机实拍成功（LLM 读 SKILL.md → 工具链 → 任务 `succeeded`）。
- **F 端到端总体**：`succeeded`，落点误差 0.0353 m，产出 MP4；负例 `OBSERVATION_STALE / MOTION_NOT_ALLOWED / PLAN_TAMPERED / PLAN_NOT_FOUND` 全部 fail-closed。

## 真实模型（VLM）parity

`deepseek-v4-flash-vision-exp`（经 `REKEP_VLM_BASE_URL` 指定网关）：
- 用 `XERA_API_KEY_1` 可访问；`XERA_API_KEY_2` 对该模型返回 403（未使用其它模型）。
- 需 `max_tokens ≥ 16384`：该推理模型的 `reasoning_content` 会吃光 2048 token 导致 `content` 为空、`finish_reason=length`。
- 加入与参考 real 路径一致的 **index guard**（列出关键点 id↔object）后，模型正确引用 `pick_cube` 索引，执行成功。

## 复现

```bash
source scripts/env.sh
# 离线（契约/对齐/负例）：不需要网络
bash scripts/nsrun.sh bash scripts/acceptance_full.sh        # 输出 P4_ACCEPTANCE: PASS

# 带 VLM 的 Agent 端到端（需 LLM/VLM key；用 dora shim 以保留网络）
export REKEP_VLM_API_KEY=... REKEP_VLM_BASE_URL=... REKEP_VLM_MODEL=...
bash scripts/acceptance_agent_shim.sh
```

## 关键修复记录（本机）
共享 dora 端口冲突（离线用私有 netns；联网 Agent 用 `scripts/shimbin/dora` 私有端口）；glibc<2.32 chmod shim；gateway v1.1.0 拒绝 `agent` 键；`forge_tool` session 语义；execute node 惰性初始化；IK 多 seed；MuJoCo assisted grasp；**kmeans 无界死循环→有界**；**torch 默认 72 线程超订→限 4**；感知缓存 + 后台预热；VLM index guard；verification 合同 `audit`+`best_effort`。

## 待办（非阻塞，P5）
P5（可选）：当前 node 入口为宿主路径的**单文件 shell 包装**（本地安装/启动可行）；生产分发需**可移植单文件**（PyInstaller 薄冻结）与 Registry/TOS 登记发布。
